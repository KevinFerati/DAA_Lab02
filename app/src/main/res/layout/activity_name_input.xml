<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NameInputActivity">

        <EditText
            android:id="@+id/name"
            android:layout_weight="1"
            android:layout_marginHorizontal="@dimen/viewSpacing"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/saveButton"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_height="@dimen/touchTargetSize" android:layout_width="0dp"/>

        <Button
            style="@style/Widget.MaterialComponents.Button"
            android:layout_weight="0"
            app:icon="@drawable/check"
            app:iconGravity="start"
            android:id="@+id/saveButton"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/touchTargetSize"
            android:layout_marginEnd="@dimen/viewSpacing"
            android:text="@string/save" />

</androidx.constraintlayout.widget.ConstraintLayout>